// API configuration
export const API_BASE_URL = "http://localhost:8080/api";

export const ENDPOINTS = {
  // Auth
  LOGIN: `${API_BASE_URL}/student/auth/login`,
  REFRESH_TOKEN: `${API_BASE_URL}/student/auth/refresh`,
};
